# Env files: ignore all variants (they hold secrets) but keep the
# placeholder templates that are intentionally committed.
.env
.env.*
!.env.example
!.env.docker
.claude/
.playwright-mcp/
.superpowers/

# Python bytecode (scripts/uptime)
__pycache__/
*.pyc
